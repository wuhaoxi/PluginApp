package com.wu.lib_audio.mediaplayer.db;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.wu.ft_audio.mediaplayer.model.AudioBean;

import com.wu.ft_audio.mediaplayer.model.Favourite;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "FAVOURITE".
*/
public class FavouriteDao extends AbstractDao<Favourite, Long> {

    public static final String TABLENAME = "FAVOURITE";

    /**
     * Properties of entity Favourite.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property FavouriteId = new Property(0, Long.class, "favouriteId", true, "_id");
        public final static Property AudioId = new Property(1, String.class, "audioId", false, "AUDIO_ID");
    }

    private DaoSession daoSession;


    public FavouriteDao(DaoConfig config) {
        super(config);
    }
    
    public FavouriteDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"FAVOURITE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: favouriteId
                "\"AUDIO_ID\" TEXT NOT NULL );"); // 1: audioId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"FAVOURITE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Favourite entity) {
        stmt.clearBindings();
 
        Long favouriteId = entity.getFavouriteId();
        if (favouriteId != null) {
            stmt.bindLong(1, favouriteId);
        }
        stmt.bindString(2, entity.getAudioId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Favourite entity) {
        stmt.clearBindings();
 
        Long favouriteId = entity.getFavouriteId();
        if (favouriteId != null) {
            stmt.bindLong(1, favouriteId);
        }
        stmt.bindString(2, entity.getAudioId());
    }

    @Override
    protected final void attachEntity(Favourite entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Favourite readEntity(Cursor cursor, int offset) {
        Favourite entity = new Favourite( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // favouriteId
            cursor.getString(offset + 1) // audioId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Favourite entity, int offset) {
        entity.setFavouriteId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAudioId(cursor.getString(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Favourite entity, long rowId) {
        entity.setFavouriteId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Favourite entity) {
        if(entity != null) {
            return entity.getFavouriteId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Favourite entity) {
        return entity.getFavouriteId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getAudioBeanDao().getAllColumns());
            builder.append(" FROM FAVOURITE T");
            builder.append(" LEFT JOIN AUDIO_BEAN T0 ON T.\"AUDIO_ID\"=T0.\"ID\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Favourite loadCurrentDeep(Cursor cursor, boolean lock) {
        Favourite entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        AudioBean audioBean = loadCurrentOther(daoSession.getAudioBeanDao(), cursor, offset);
         if(audioBean != null) {
            entity.setAudioBean(audioBean);
        }

        return entity;    
    }

    public Favourite loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Favourite> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Favourite> list = new ArrayList<Favourite>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Favourite> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Favourite> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
